# Requirements for backend
# Recommended: create a conda environment and install the pinned scikit-learn wheel
# Example:
#   conda create -n detectenv python=3.10 -y
#   conda activate detectenv
#   conda install -c conda-forge scikit-learn=1.3.0 joblib numpy -y
# If using pip, prefer prebuilt wheels when available:
#   pip install -r requirements.txt --only-binary=:all:

# Core web framework
flask==2.3.3
flask-cors==4.0.0
requests==2.31.0

# APK analysis
androguard==3.4.0a1
cryptography==41.0.4

# Machine Learning (pre-compiled wheels)
scikit-learn==1.3.0
pandas>=1.3.0
numpy>=1.20.0
xgboost>=1.5.0
joblib>=1.0.0